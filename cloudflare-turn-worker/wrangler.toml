name = "shadowdark-turn"
main = "worker.js"
compatibility_date = "2026-06-01"

# Non-secret config. TURN_KEY_ID is an identifier (can't mint creds without the
# secret token), so it's safe here. If you'd rather not commit it, delete the
# line and run `wrangler secret put TURN_KEY_ID` instead.
[vars]
TURN_KEY_ID = "9c9a77f370ba22474f5d71a135583f2d"
ALLOW_ORIGINS = "https://gustavogomez092.github.io"
TURN_TTL = "86400"

# Secret — do NOT put here. Set it once with:
#   wrangler secret put TURN_API_TOKEN
